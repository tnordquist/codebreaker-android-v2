<resources>
  <!-- Base application theme. -->
  <style name="AppTheme" parent="Theme.MaterialComponents.Light">
    <!-- Customize your theme here. -->
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="colorAccent">@color/colorAccent</item>
  </style>

  <style name="AppTheme.NoActionBar">
    <item name="windowActionBar">false</item>
    <item name="windowNoTitle">true</item>
  </style>

  <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

  <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

  <style name="GuessResultText">
    <item name="android:includeFontPadding">false</item>
    <item name="android:textSize">@dimen/result_text_size</item>
  </style>

  <style name="GuessNumber">
    <item name="android:textSize">@dimen/guess_number_size</item>
    <item name="android:textAlignment">viewEnd</item>
    <item name="android:includeFontPadding">false</item>
    <item name="android:paddingEnd">@dimen/guess_number_padding</item>
  </style>

</resources>